# GhostDrive
GhostDrive is an experimental virtual filesystem that stores all of its data inside a video file. Every frame of the video acts as a block of the filesystem, allowing the video to be mounted, read, and written like a real disk.  This project is currently a work in progress.
